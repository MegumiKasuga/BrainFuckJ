+*  # 跳转到标记 1
$  # 标记 0
|> [<+>-]  # 内存指针置为1, 循环进行 {mem[0] + 1, mem[1] - 1} 直至 mem[1] == 0
|  # 内存指针归 0
}  # 弹栈

$  # 标记 1
@ ++ > +(( >  # mem[0]归0, mem[0] = 2, mem[1] = 4
{  # 跳转到标记(mem[2] = 0)，即标记0
.  # 弹栈后应当回到这里并输出mem[0]的值